import { BuildStepDirective } from './build-step.directive';

describe('BuildStepDirective', () => {
  it('should create an instance', () => {
    const directive = new BuildStepDirective();
    expect(directive).toBeTruthy();
  });
});
